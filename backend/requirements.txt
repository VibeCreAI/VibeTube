# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# ML models
torch>=2.7.0
transformers>=4.36.0,<=4.57.6
accelerate>=0.26.0
huggingface_hub>=0.20.0
qwen-tts>=0.0.5
diffusers>=0.30.0
safetensors>=0.4.0

# LuxTTS (voice cloning engine)
--find-links https://k2-fsa.github.io/icefall/piper_phonemize.html
linacodec @ git+https://github.com/ysharma3501/LinaCodec.git
Zipvoice @ git+https://github.com/ysharma3501/LuxTTS.git

# Chatterbox TTS sub-dependencies.
# Note: install `chatterbox-tts` separately with `pip install --no-deps chatterbox-tts`
# because its pinned dependency versions conflict with the rest of the stack.
conformer>=0.3.2
omegaconf
pykakasi
resemble-perth>=1.0.1
s3tokenizer
spacy-pkuseg
pyloudnorm
torchaudio

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
numba>=0.60.0,<0.61.0
pedalboard>=0.9.0

# Utilities
python-multipart>=0.0.6
Pillow>=10.0.0
httpx>=0.27.0
